#include "Workout-Volume.h"
#include "Weight-lifting.h"
#include <iostream>

using namespace std;

int main() {
	/*Weightlifting Weight_1("Deadlift", 25, 120, 5, 8);
	WorkoutVolume volumeCalculator;
	volumeCalculator.calculateVolume(Weight_1);

	Note:

	This is just an example, I will add the functionality to the main file later
	by creating more objects and calling the calculateVolume function.

	Pushing code to GitHub for now.

	*/
	return 0;

}